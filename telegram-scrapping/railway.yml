version: '1.0'
packages:
  - python3
  - pip
  - libpq-dev
  - libffi-dev
env:
  # Add your environment variables here, if any
tasks:
  build:
    image: 'python:3.9'
    script:
      - pip install -r requirements.txt
      - pip install python-telegram-bot==13.7 # Install python-telegram-bot
      # Add any other necessary build commands here
  start:
    command: python main.py